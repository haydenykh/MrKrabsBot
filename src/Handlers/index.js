const { loadEvents } = require("./LoadEvents.js");
const { loadCommands } = require("./LoadCommands.js");

module.exports = {
    loadEvents,
    loadCommands,
    // loadButtons,
    // loadStringSelects,
    // loadContextMenus,
};
